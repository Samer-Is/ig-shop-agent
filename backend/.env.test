# Test Environment for Database Testing
# This will use a temporary in-memory database for testing

# Database Configuration for Testing
DATABASE_URL=postgresql://postgres:password@localhost:5432/igshop_test

# Minimal required environment variables for testing
OPENAI_API_KEY=test-key
META_APP_ID=test-app-id
META_APP_SECRET=test-app-secret
META_WEBHOOK_VERIFY_TOKEN=test-webhook-token
JWT_SECRET_KEY=test-jwt-secret-key-for-testing-only

# Test environment
ENVIRONMENT=test
DEBUG=true 